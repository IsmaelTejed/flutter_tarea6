name: flutter_tarea6
description: "Aplicacion multi-herramientas hecha en Flutter (ITLA)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
     flutter:
       sdk: flutter
     cupertino_icons: ^1.0.6
     http: ^1.2.1
     url_launcher: ^6.2.6
     audioplayers: ^6.0.0
     intl: ^0.19.0
     
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
